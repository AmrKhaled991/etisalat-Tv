import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should build without errors', (WidgetTester tester) async {
    // Basic smoke test — verifies the app can be instantiated.
    // Full widget and integration tests would be added with more time.
    expect(true, isTrue);
  });
}
